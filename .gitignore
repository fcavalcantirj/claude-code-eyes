# Secrets & local camera config — never commit these
.env
.env.*
.cce.env
*.token
secrets*

# Captured frames (snap.sh writes to ./.claude-code-eyes)
.claude-code-eyes/
frame-*.jpg
frame-*.jpeg
frame-*.png
frames/

# OS / editor cruft
.DS_Store

# Local launch / planning docs — not part of the public skill
LAUNCH.md
handovers/
